#!/usr/bin/env bash

set -e

git pull
VERSION=$(bin/console sdk:increase-version -c)
bin/tools/composer-update
git add . && git commit -am "auto commit" && git push
git tag "$VERSION"
git push --tags


